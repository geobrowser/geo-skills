{
  "name": "geo-publish-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Self-contained dependencies for the geo-publish skill. Install once inside this directory; scripts are invoked from user projects.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@geoprotocol/geo-sdk": "0.20.1",
    "viem": "^2.37.6"
  }
}
